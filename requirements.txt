# Local development convenience: install the package with every component extra.
#   pip install -r requirements.txt
#
# Dependencies are declared in pyproject.toml under [project.optional-dependencies],
# one extra per Cloud Run deployable. Deployment installs only the relevant extra
# (see deploy/Dockerfile: pip install ".[${COMPONENT}]").
-e .[profiles,weblinks,ingest,api,frontend,dev]
